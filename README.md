# Care Partner Copilot Landing Page

Static responsive landing page.

## Local preview
Open `index.html`, or run:

```bash
python -m http.server 8080
```

Then visit `http://localhost:8080`.

## Before publishing
Replace `hello@carepartnercopilot.com` with your real email or booking link.
